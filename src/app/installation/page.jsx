import React from 'react';

export const metadata = {
  title: "Installation Page",
};

const installation = () => {
    return (
        <div>
            Installation Page
        </div>
    );
};

export default installation;